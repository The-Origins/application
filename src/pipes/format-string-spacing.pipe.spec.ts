import { FormatStringSpacingPipe } from './format-string-spacing.pipe';

describe('FormatStringSpacingPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatStringSpacingPipe();
    expect(pipe).toBeTruthy();
  });
});
